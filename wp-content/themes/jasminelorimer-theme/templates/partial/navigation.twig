<nav class="navbar navbar-expand-xl navbar-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mainLinks" aria-controls="mainLinks" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="col collapse navbar-collapse" id="mainLinks">
        <ul class="navbar-nav mr-auto">
            {% for nav_item in mainNav %}
                {% if function('is_page', nav_item.slug) %}
                <li class="nav-item active">
                {% else %}
                <li class="nav-item">
                {% endif %}
                    <a href="{{ nav_item.link }}" class="nav-link">
                        {{ nav_item.name }}
                        {% if function('is_page', nav_item.slug) %}
                            <span class="sr-only">(current)</span>
                        {% endif %}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>

    <div class="col-auto">
        <h1 class="hdr-logo" role="banner">
            <a class="hdr-logo-link" href="{{site.url}}" rel="home">{{site.name}}</a>
        </h1>
    </div>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#socialLinks" aria-controls="socialLinks" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="col collapse navbar-collapse" id="socialLinks">
        <div class="d-flex w-100 justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">Contact</a>
                </li>
            </ul>
            <ul class="navbar-nav social-nav">
                {# <li class="nav-item">
                    <a class="nav-link" href="#" target="_blank">
                        <i class="fa fa-facebook"></i>
                        <span class="sr-only">Facebook</span>
                    </a>
                </li> #}
                <li class="nav-item">
                    <a class="nav-link" href="https://www.instagram.com/jasmine.lorimer" target="_blank">
                        <i class="fa fa-instagram"></i>
                        <span class="sr-only">Instagram</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://twitter.com/jazlor1" target="_blank">
                        <i class="fa fa-twitter"></i>
                        <span class="sr-only">Twitter</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://www.youtube.com/channel/UCc6MemdlxFOQybEjitYuDsg" target="_blank">
                        <i class="fa fa-youtube-play"></i>
                        <span class="sr-only">YouTube</span>
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa fa-search"></i>
                        <span class="sr-only">Search</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>